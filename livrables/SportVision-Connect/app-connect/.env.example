# Copier en .env.local (jamais commité) — même projet Supabase que Club+ (app-next) et l'app
# vanilla : une seule identité utilisateur partagée entre les trois applications.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Usage serveur uniquement (jamais importé côté client). Non utilisé en Phase 1 — aucune écriture
# ne doit passer par la clé service role, toujours par le client authentifié + RLS.
SUPABASE_SECRET_KEY=
