{
  "name": "sportvision-connect-app",
  "version": "1.0.0",
  "private": true,
  "description": "Emballage Capacitor (iOS + Android) de SportVision Connect — ne contient aucun code applicatif : server.url (capacitor.config.json) charge directement https://connect.sportvision-an.fr, le vrai site app-connect déployé. Aucune copie locale du code à synchroniser.",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap run ios",
    "android": "cap run android",
    "assets": "capacitor-assets generate"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/android": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@capacitor/assets": "^3.0.5"
  }
}
