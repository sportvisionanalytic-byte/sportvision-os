# Copier en .env.local (jamais commité) et renseigner avec les valeurs du .env racine du repo —
# même projet Supabase que l'app vanilla SportVision-Connect/app.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Usage serveur uniquement (jamais importé côté client). Non utilisé en Phase 1 — aucune écriture
# ne doit passer par la clé service role, toujours par le client authentifié + RLS.
SUPABASE_SECRET_KEY=
